# @vitejs/plugin-react@4 lists Vite <=7 in its peer range, but works fine with
# the Vite 8 (rolldown) build used here. Allow the install to proceed so that
# `npm ci` (strict by default) matches what `npm install` already does locally.
legacy-peer-deps=true
